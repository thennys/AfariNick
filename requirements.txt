# Core application
Django==5.0.6
pandas==2.2.2          # CSV/Excel parsing for the harvest-import bonus
openpyxl==3.1.5        # Excel (.xlsx) support for pandas

# Production serving & database
gunicorn==22.0.0       # WSGI server used by Render (start command)
whitenoise==6.7.0      # serves static files from the app (Render + Vercel)
dj-database-url==2.2.0 # parses DATABASE_URL, e.g. a Neon Postgres connection string
psycopg2-binary==2.9.9 # PostgreSQL driver (Neon / any Postgres). Not used on SQLite.
